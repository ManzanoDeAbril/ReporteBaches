using System;
using Microsoft.Maui.Controls;

namespace ReporteBaches.app.Views;

/// <summary>
/// Código de respaldo C# (code-behind) de LoginPage.xaml.
/// 'partial' indica que se unirá con el código generado automáticamente a partir del diseño XAML.
/// </summary>
public partial class LoginPage : ContentPage
{
    public LoginPage()
    {
        // 'InitializeComponent()' es un método autogenerado que lee el diseño XAML
        // y construye todos los botones, entradas y etiquetas en la pantalla.
        InitializeComponent();
    }
}